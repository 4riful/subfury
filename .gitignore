__pycache__/
*.pyc

# large / generated artifacts
data/cc/
data/groups_*.jsonl
results/**/*.pt
results/train.log

# the tokenizer that ships with the model. results/tokenizer.json is the
# byte-identical source copy tokenizer_train.py writes and train.py copies in,
# so only the shipped one is tracked.
!results/subfury/tokenizer.json
results/tokenizer.json

# webui runtime
webui/__pycache__/

# third-party wordlists (fetched, see README)
data/subdomains_*.txt

# fetched/derived caches — reproducible, and large
research/data/cache/
results/research/cache/
results/runs/smoke/
